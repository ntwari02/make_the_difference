🚀 QUICK REFERENCE - Never Get EADDRINUSE Error Again!

═══════════════════════════════════════════════════════════════

🎯 DAILY WORKFLOW (Use this every day):
   daily-workflow.bat

📥 SAFE PULL (Use this to pull changes):
   pull-changes.bat

🛠️ SERVER MANAGEMENT:
   server/start-server.bat    - Start server (kills conflicts first)
   server/start-dev.bat       - Start in development mode
   server/kill-server.bat     - Kill any server using port 3001

⚡ QUICK COMMANDS:
   cd server && npm start     - Start server
   cd server && npm run dev   - Start with auto-restart
   cd server && kill-server.bat - Kill server

🔧 EMERGENCY (If still getting errors):
   1. Run: server/kill-server.bat
   2. Wait 5 seconds
   3. Run: cd server && npm start

═══════════════════════════════════════════════════════════════

✅ WHAT WE FIXED:
- Server now checks port availability before starting
- Auto-cleanup kills conflicting processes
- Graceful shutdown on Ctrl+C
- Automated pull script handles everything
- Development mode with auto-restart

✅ THE ERROR WILL NEVER HAPPEN AGAIN!

═══════════════════════════════════════════════════════════════
