# Copy to .env and fill values

# Server
PORT=3000
NODE_ENV=production
SESSION_SECRET=change_me
DEV_USE_MEMORY_SESSION=false

# Database (AWS RDS MySQL)
DB_HOST=your-rds-endpoint.rds.amazonaws.com
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=strong_password
DB_NAME=mbappe
DB_SSL=true
DB_CONNECT_TIMEOUT=30000
DB_SLOW_MS=300

# JWT
JWT_SECRET=change_this_jwt_secret

# Email (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_user
SMTP_PASSWORD=your_password
SMTP_FROM="Make The Difference <no-reply@example.com>"

# Cloudinary
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Uploads
UPLOADS_DIR=public/uploads/profiles
UPLOADS_PUBLIC_PATH=/uploads/profiles
ATTACHMENTS_STORAGE=disk
UPLOAD_ROOTS=
UPLOAD_WRITE_ROOT=
UPLOAD_SUBFOLDERS_EXTRA=

# Ads / media options
AD_VIDEO_REQUIRE_AUDIO=true
